export type BdbBoat = {
  id: string
  name: string
  label: string
}

export type BdbDevice = {
  id: string
  name: string
  label: string
  type: string
}

export type BdbWidget = {
  id: string
  type: string
  title: string
  sizeX: number
  sizeY: number
  index: number
}
